[pytest]
addopts = -p no:playwright -p no:base-url

testpaths =
    tests
    test_sep_parser.py

python_files =
    test_*.py

norecursedirs =
    .venv
    .venv-1
    build
    dist
    output
    test-results
    data
    Material_analisado
    twin
    scripts